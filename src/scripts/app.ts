/**
 * app.ts — IdeaBrief Client-Side Interactivity
 *
 * This script is embedded inline in BaseLayout.astro.
 * This file exists as a reference / source of truth.
 *
 * Features:
 * - Theme initialization (dark by default)
 * - Scroll reveal animations (JS fallback for scroll-driven animations)
 * - Score bar animation on the idea card
 * - Smooth scroll for anchor links
 * - Header shadow on scroll
 * - Vault card stagger animation
 * - Pricing card hover glow (desktop only)
 */

export {};
